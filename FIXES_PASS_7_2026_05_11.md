# Fixes Pass 7 — 2026-05-11

## Scope

Fresh small-things production-readiness sweep after the prior payment, webhook,
cron, tenant-scope, and production smoke-test passes.

## Confirmed Fixes

- Fixed shared Prettier config resolution. `pnpm format:check` previously
  crashed because the root Prettier process could not resolve
  `prettier-plugin-tailwindcss`; `tooling/prettier/index.js` now resolves the
  plugin from the config package.
- Closed current production dependency advisories from `pnpm audit --prod`:
  - Next.js server-components DoS advisory by allowing patched Next `15.5.16+`
    and installing `15.5.18`.
  - PostCSS stringify advisory by pinning patched `postcss@8.5.12`.
  - `uuid` buffer-bounds advisory by pinning patched `uuid@11.1.1`.
  - `fast-xml-parser` delimiter-injection advisory by pinning patched
    `fast-xml-parser@5.7.0`.
- Documented `CLOUDFLARE_API_TOKEN` as deploy-only/local-wrangler automation
  config in `.env.example` and `ENV.md`; the Worker runtime does not read it.
- Corrected `ENV.md` drift against runtime code:
  - Ziina production API base URL includes the required `/api` suffix.
  - `ENCRYPTION_KEY` accepts either 64 hex chars or 44 base64 chars, matching
    `packages/db/src/crypto.ts`.

## Checks Run

- `pnpm install` — updated local `node_modules` to the patched lockfile.
- `pnpm audit --prod` — passed, no known vulnerabilities.
- `npx turbo typecheck --force` — passed.
- `npx turbo lint --force` — passed with the existing Playwright
  `no-console` warning in `apps/web/e2e/health.smoke.spec.ts`.
- `npx turbo build --force` — passed using installed Next.js `15.5.18`.
- `pnpm --filter @equestrian/shared test` — passed, 112 tests.
- `pnpm --filter @equestrian/db test` — passed, 105 tests.
- Debug/focused-test marker scan found no actual `debugger` statements or
  `.only(` focused tests.
- Tracked artifact scan found no tracked local env files or build artifacts
  beyond the committed `.env.example` template.

## Still Not Fully Clean

- `pnpm format:check` now runs, but still fails with 319 existing formatting
  warnings across the repo. I did not mass-format hundreds of unrelated files in
  this pass because that would create a very large cosmetic diff.
- Local env files intentionally omit context-specific keys from `.env.example`
  (mobile build keys, Sentry alert knobs, optional webhook tuning, etc.). After
  documenting `CLOUDFLARE_API_TOKEN`, there are no local-only env keys relative
  to `.env.example`.
- This pass did not deploy the dependency update. Production remains on the
  previously deployed Worker until a new deploy is run and smoked.
- Payment certification remains externally blocked until licensed
  merchant/provider accounts complete real payment, refund, and webhook flows.
