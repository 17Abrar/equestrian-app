# Fixes Pass 8 — 2026-05-11

## Scope

Repository-wide formatting cleanup to resolve the remaining hygiene issue from
Fixes Pass 7.

## Confirmed Fixes

- Ran the configured root formatter over the repository:
  `pnpm format`.
- Resolved the remaining `pnpm format:check` failure. The formatter initially
  left `INCIDENT_RUNBOOK.md` needing one more pass, so that file was formatted
  again directly and rechecked.
- Confirmed no tracked local env files, build artifacts, test reports, coverage
  directories, `.next`, or `.open-next` outputs were introduced.
- Reviewed the secret-pattern diff output. Hits were documentation placeholders,
  examples, validation strings, or existing redaction tests; no concrete secret
  values were added.

## Checks Run

- `pnpm format:check` — passed.
- `git diff --check` — passed.
- `pnpm audit --prod` — passed, no known vulnerabilities.
- `npx turbo typecheck --force` — passed.
- `npx turbo lint --force` — passed with the existing Playwright
  `no-console` warning in `apps/web/e2e/health.smoke.spec.ts`.
- `npx turbo build --force` — passed using installed Next.js `15.5.18`.
- `pnpm --filter @equestrian/shared test` — passed, 112 tests.
- `pnpm --filter @equestrian/db test` — passed, 105 tests.

## Still Not Fully Verified

- This cleanup is local to the `fix/small-audit-findings` branch until pushed
  and deployed.
- Runtime production paths were not re-smoked after this formatting-only pass.
- Payment certification remains externally blocked until licensed
  merchant/provider accounts complete real payment, refund, and webhook flows.
